apiVersion: v1
kind: Service
metadata:
  name: knexit-web
  namespace: knexit
spec:
  selector:
    app: knexit-web
  ports:
  - name: http
    port: 3000
    targetPort: http
    nodePort: 30080
  type: NodePort
