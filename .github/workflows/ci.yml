name: Construção do projeto
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build (Next.js)
    runs-on: ubuntu-latest

    steps:
      - name: Checar código
        uses: actions/checkout@v4

      - name: Configurar Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Verificar segredos do Supabase
        run: |
          test -n "${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}" || (echo "Faltando secret: NEXT_PUBLIC_SUPABASE_URL" && exit 1)
          test -n "${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}" || (echo "Faltando secret: NEXT_PUBLIC_SUPABASE_ANON_KEY" && exit 1)
          test -n "${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" || (echo "Faltando secret: SUPABASE_SERVICE_ROLE_KEY" && exit 1)
          test -n "${{ secrets.SUPABASE_ACCESS_TOKEN }}" || (echo "Faltando secret: SUPABASE_ACCESS_TOKEN" && exit 1)
          echo "Secrets presentes (tamanhos mascarados)."

      - name: Instalar dependências
        run: npm ci --no-audit --no-fund

      - name: Diagnóstico de acesso Supabase
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          NEXT_TELEMETRY_DISABLED: "1"
          CI: "true"
        run: npm run db:types:doctor

      - name: Verificar drift dos tipos do Supabase
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_PROJECT_ID: ${{ secrets.SUPABASE_PROJECT_ID }}
          SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
          NEXT_TELEMETRY_DISABLED: "1"
          CI: "true"
        run: npm run db:types:check

      - name: Construir aplicação
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          NEXT_TELEMETRY_DISABLED: "1"
          CI: "true"
        run: npm run build
