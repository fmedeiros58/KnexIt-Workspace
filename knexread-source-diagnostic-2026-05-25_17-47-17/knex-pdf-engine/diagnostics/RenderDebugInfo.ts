export type RenderDebugInfo = {
  pageNumber: number;
  cssWidth: number;
  cssHeight: number;
  canvasWidth: number;
  canvasHeight: number;
  outputScale: number;
  renderQuality: string;
  backend: string;
};
