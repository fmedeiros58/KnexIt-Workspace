import type { ProcessingState } from "../bridges/contracts/processing-state";

export async function loadContextPersonalityBridge(state: ProcessingState): Promise<ProcessingState> {
  return state;
}
